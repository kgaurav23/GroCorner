package `in`.grocorner.ui.login.navigator

import android.content.Context

class LoginNavigator {

    companion object {
        fun navigateToSRPActivity(context: Context) {
            //context.startActivity(Intent(context, OnBoardingActivity::class.java))
        }
    }
}